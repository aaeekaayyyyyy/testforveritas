# signature

The documentation critically omits a required input parameter (`decimal: int`), making the described function signature incompatible with the actual code's signature. A user relying on the documentation would encounter runtime errors when attempting to call the function.

## Example

```python
Required parameter: `decimal: int`
```

## Notes

> Update the documentation to include the required `decimal: int` parameter, ensuring it matches the code's function signature.
