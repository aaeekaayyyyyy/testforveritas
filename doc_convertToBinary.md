# convertToBinary

This function converts a number to binary representation.

### Returns
- `str`: The binary representation as a string.

### Examples
```python
convert_to_binary(10)  // Returns "1010"
convert_to_binary(0)   // Returns "0"
```
